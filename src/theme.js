// Re-export from shared theme — single source of truth.
// Edit /shared/theme.js to change colours, fonts or shadows across both apps.
export {
  C, serif, sans, FONTS_URL,
  GRAD, CARD_SHADOW, CARD_SHADOW_SM, CARD_SHADOW_XS,
  shadow, shadowSm, shadowXs,
  radius,
} from '@shared/theme';
